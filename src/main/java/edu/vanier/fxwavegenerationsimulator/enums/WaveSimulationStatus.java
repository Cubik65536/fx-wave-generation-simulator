package edu.vanier.fxwavegenerationsimulator.enums;

/**
 * An enum listing all status for the wave simulation (playing, paused, stopped).
 */
public enum WaveSimulationStatus {
    PLAYING,
    PAUSED,
    STOPPED
}
